#include <stdio.h>

/**
 * main - Print the numbers 0-9 on separate lines.
 *
 * Return: Always 0.
 */
int main(void)
{
        int i;

        for (i = 0; i < 10; i++)
        {
                printf("%d\n", i);
        }

        return (0);
}
